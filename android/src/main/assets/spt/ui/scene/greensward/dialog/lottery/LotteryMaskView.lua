local LotteryMaskView = class(unity.base)

function LotteryMaskView:ctor()
--------Start_Auto_Generate--------
    self.moveRectTrans = self.___ex.moveRectTrans
--------End_Auto_Generate----------
    self.cam = self.___ex.cam
end

return LotteryMaskView
