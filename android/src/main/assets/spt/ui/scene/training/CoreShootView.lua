local BaseCoreTrainView = require("ui.scene.training.BaseCoreTrainView")

local CoreShootView = class(BaseCoreTrainView)

function CoreShootView:ctor()
    CoreShootView.super.ctor(self)
end

return CoreShootView
